[
  {
    "name": "${project}-${environment}-api-${sfx}",
    "image": "${repository_url}:latest",
    "portMappings": [
      {
        "containerPort": 3001,
        "hostPort": 3001,
        "protocol": "tcp"
      }
    ],
    "cpu": 0,
    "environment": [],
    "secrets": [],
    "volumesFrom": [],
    "readonlyRootFilesystem": false,
    "essential": true,
    "environmentFiles": [],
    "stopTimeout": 30,
    "networkMode": "awsvpc",
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "/ecs/${project}-${environment}-api-${sfx}",
        "awslogs-region": "${region}",
        "awslogs-stream-prefix": "ecs"
      }
    }
  }
]
