[
  {
    "name": "${project}-${environment}-frontend-task-${sfx}",
    "image": "${repository_url}:latest",
    "portMappings": [
      {
        "containerPort": 3000,
        "hostPort": 3000,
        "protocol": "tcp"
      }
    ],
    "cpu": 0,
    "environment": [],
    "secrets": [],
    "volumesFrom": [],
    "readonlyRootFilesystem": false,
    "essential": true,
    "environmentFiles": [],
    "stopTimeout": 30,
    "networkMode": "awsvpc",
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "/ecs/${project}-${environment}-frontend-log-group-${sfx}",
        "awslogs-region": "${region}",
        "awslogs-stream-prefix": "ecs"
      }
    }
  }
]
